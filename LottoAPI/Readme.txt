Hello,
This API used ASP.Net Core 2.2. It runs on IIS and uses a SQL Server database.
The API can be accessed using Swagger here : https://localhost:44309/swagger/index.html (replace the port number with your own port number)